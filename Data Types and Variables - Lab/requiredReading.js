function solve(numberOfPages,pagesRead,numberOfDays) {

    let totalTime = numberOfPages / pagesRead;
    let requiredHoursPerDay = totalTime / numberOfDays;
    console.log(requiredHoursPerDay);



}
solve(212,20,2 )